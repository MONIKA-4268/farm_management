/* General resets and base */
*,
*::before,
*::after {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(120deg, #e0f7fa 0%, #ffe0b2 100%);
  color: #212529;
  line-height: 1.5;
  min-height: 100vh;
}

/* Navbar */
.navbar {
  background: #fff3e0 !important;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
}

.navbar-brand {
  color: #388e3c !important;
  font-weight: bold;
  font-size: 2rem;
  letter-spacing: 2px;
}

.nav-link {
  color: #777;
  transition: color 0.3s ease;
}

.nav-link.active,
.nav-link:hover {
  color: #338851 !important;
  font-weight: bold;
}

.navbar-toggler {
  border: none;
}

.navbar-toggler-icon {
  background-image: url("data:image/svg+xml;charset=utf8,%3Csvg viewBox='0 0 30 30' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='rgba%2824,63,24, 0.7%29' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3E%3C/svg%3E");
}

/* Tab sections */
.tab-section {
  display: none;
  animation: fadeInDown 0.4s ease forwards;
  padding-top: 1rem;
  min-height: 60vh;
  background: none;
}

.active-section {
  display: block !important;
}

@keyframes fadeInDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Tables */
table {
  width: 100%;
  border-collapse: collapse;
}

table thead tr th {
  padding: 12px 8px;
  background-color: #f5f5f5;
  border: 1px solid #ddd;
  font-weight: 600;
  color: #444;
  text-align: center;
}

table tbody tr td {
  padding: 8px;
  border: 1px solid #eee;
  text-align: center;
  color: #555;
}

/* Forms */
form.form-inline input,
form.form-inline select {
  min-width: 120px;
  margin-right: 8px;
  margin-bottom: 8px;
  padding: 8px 10px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

form.form-inline button {
  padding: 8px 16px;
  border-radius: 4px;
}

form.form-inline button.btn-success {
  background-color: #388e3c;
  color: #fff;
  border: none;
  transition: background 0.3s ease;
}

form.form-inline button.btn-success:hover {
  background-color: #2e6b2b;
}

form.form-inline button.btn-warning {
  background-color: #f0ad4e;
  color: #fff;
  border: none;
  transition: background 0.3s ease;
}

form.form-inline button.btn-warning:hover {
  background-color: #d69521;
}

form.form-inline button.btn-danger {
  background-color: #d9534f;
  color: #fff;
  border: none;
  transition: background 0.3s ease;
}

form.form-inline button.btn-danger:hover {
  background-color: #b43634;
}

input.form-control {
  padding: 8px 12px;
  border-radius: 6px;
  border: 1px solid #ccc;
  width: 100%;
  box-sizing: border-box;
  color: #444;
}

input.form-control::placeholder {
  color: #888;
  font-style: italic;
}

/* Responsive for mobile */
@media (max-width: 576px) {
  form.form-inline input,
  form.form-inline select,
  form.form-inline button {
    width: 100%;
    margin-right: 0;
  }
}

/* Buttons */
.btn {
  border-radius: 5px;
  font-weight: 600;
  cursor: pointer;
}

/* Logout Button */
button#logout-btn {
  border: 1.5px solid #dc3545;
  background: transparent;
  color: #dc3545;
  padding: 6px 12px;
  font-weight: 600;
}

button#logout-btn:hover {
  background: #dc3545;
  color: #fff;
}

/* Language toggle and other UI elements */
#lang-toggle {
  margin-bottom: 15px;
  cursor: pointer;
}

/* Alerts and messages */
.text-danger {
  color: #dc3545;
}

/* Logo in Navbar */
.navbar .navbar-brand img {
  height: 48px;
  margin-right: 12px;
  vertical-align: middle;
}

/* Utility classes */
.text-center {
  text-align: center !important;
}

.mb-2 {
  margin-bottom: 0.5rem !important;
}

.mb-3 {
  margin-bottom: 1rem !important;
}

.mb-4 {
  margin-bottom: 1.5rem !important;
}

.mt-2 {
  margin-top: 0.5rem !important;
}

.mt-3 {
  margin-top: 1rem !important;
}
